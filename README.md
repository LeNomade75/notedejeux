# Critiques du serveur

1. `npm install`
2. Copier `.env.example` en `.env` et remplir les variables.
3. Discord Developer Portal : ajouter le redirect `http://localhost:3000/api/auth/callback/discord`.
4. `npx prisma db push` puis `npm run dev`.
